# OpenBridge Releases

This repository hosts public beta release artifacts for OpenBridge.

The source code and product work live in the private `lunr-studio/OpenBridge` repository during beta. This release-only repository should contain release notes, DMG assets, and checksum files only.

Do not add source code, invite codes, private support logs, credentials, or development artifacts here.

